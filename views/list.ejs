<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Posts List</title>
    <link rel="stylesheet" type="text/css" href="/css/list.css">
    <link rel="icon" href="/png/favicon.png">
</head>
<body>
    <h1>Posts List</h1>
    <p>身份: <%=username%> </p>
    <form method="post">
        <button name="new">New Post</button>
        <button name="back">Back Home</button>
        <button name="pre"><<</button>
        <button name="next">>></button>
    </form>

    <hr>
    <div>
        <% for(var i in result) { %>
            <h2><%= result[i].title %></h2>
            <p>Author: <%= result[i].author %></p>
            <p>--</p>
            <pre class="msg"><%= result[i].message %></pre>
            <p>Comment: <%= result[i].comment %></p>
            <p>Date: <%= result[i].date %></p>
            <form method="post">
                <button name="read" value=<%= result[i].postId %>  >Read Post</button>
                <button name="edit" value=<%= result[i].postId %>  >Edit Post</button>
                <button name="remove" value=<%= result[i].postId %>  >Remove Post</button>
            </form>
            <hr>
        <% } %>
    </div>


    <form method="post">
        <button name="new">New Post</button>
        <button name="back">Back Home</button>
        <button name="pre"><<</button>
        <button name="next">>></button>
    </form>
    <hr>

</body>
</html>